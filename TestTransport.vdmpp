class TransportTest

operations
    private assertTrue: bool ==> ()
        assertTrue(cond) == return
    pre cond;
        
    private createTransport: () ==> ()
    createTransport() ==
    (
        dcl transport : Transport := new Transport(<Ambulance>);
    
        -- Asserts
        assertTrue( transport.type  = <Ambulance> );
    );

    public static main: () ==> ()
    main() ==
    (
        new TransportTest().createTransport();
    );
    
end TransportTest